import app from './app';
// yarn sucrase-node nome.arquivo
app.listen(3333); // localhost::3333
// app.listen(3333, 'localhost'); // segundo parametro limita as formas de acesso para o dns "localhost"
